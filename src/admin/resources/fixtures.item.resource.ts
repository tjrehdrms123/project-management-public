const fixturesItemResource = (model) => {
    return {
      resource: model,
      options: {
        parent : {
          name : '사내관리',
        },
      }
    }
  }
  
  export default fixturesItemResource