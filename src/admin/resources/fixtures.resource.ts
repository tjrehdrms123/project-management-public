const fixturesResource = (model) => {
    return {
      resource: model,
      options: {
        parent : {
          name : '사내관리',
        },
      }
    }
  }
  
  export default fixturesResource